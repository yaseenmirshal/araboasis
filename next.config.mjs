/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ["images.unsplash.com"],
  },
  /* config options here */
  reactCompiler: true,
};

export default nextConfig;
